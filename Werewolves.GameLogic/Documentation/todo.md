# todo: 
 
## add helper functions to validate expected input types 

## change phase transition info to define source target phase explicitely
instead of having it defined in the build phase definitions dictionary for each phase, get a single dictionary that contains all the information for transitions: start state, end state, transition reason.

## verify all log usages
ensure everything goes through game session now, particularly roles