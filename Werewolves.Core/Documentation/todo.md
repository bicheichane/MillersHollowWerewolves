todo:

- Centralize Night wake up order, this is going to get unwieldy if it remains spread out among role implementations
- remove state from GameSession like "who is the sheriff" "who are the lovers" etc., leave that up to searching the game logs/player list
- separate witch potion usages into separate actions
- invalidinput_typemismatch enum is duplicated